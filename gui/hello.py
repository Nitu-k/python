from tkinter import*
#widget=gui elemt: button, lable, image
#window= serves as a container to hold or contain widgets
window=Tk() #instentiate an instance of window 
window.geometry("420x420")
window.title("Nitu's GUI")
window.config(background="black")

window.mainloop() #place window on screen
